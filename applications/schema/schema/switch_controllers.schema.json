{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Switch Controllers",
  "description": "Switch one or several controllers of a hardware interface",
  "definitions": {
    "interface": {
      "title": "Interface",
      "description": "The name of the desired hardware interface",
      "additionalProperties": false,
      "type": "string"
    },
    "start": {
      "title": "Start Controllers",
      "description": "The name(s) of the controller(s) to start",
      "additionalProperties": false,
      "oneOf": [
        {
          "type": "array",
          "minItems": 1,
          "items": {
            "title": "Controller Names",
            "description": "The names of the controllers to start",
            "type": "string"
          }
        },
        {
          "title": "Controller Name",
          "description": "The name of the controller to start",
          "type": "string"
        }
      ]
    },
    "stop": {
      "title": "Stop Controllers",
      "description": "The name(s) of the controller(s) to stop",
      "additionalProperties": false,
      "oneOf": [
        {
          "type": "array",
          "minItems": 1,
          "items": {
            "title": "Controller Names",
            "description": "The names of the controllers to stop",
            "type": "string"
          }
        },
        {
          "title": "Controller Name",
          "description": "The name of the controller to stop",
          "type": "string"
        }
      ]
    },
    "switch_controllers_object": {
      "title": "Switch Controllers",
      "description": "Switch one or several controllers of a hardware interface",
      "type": "object",
      "properties": {
        "interface": {
          "$ref": "#/definitions/interface"
        },
        "start": {
          "$ref": "#/definitions/start"
        },
        "stop": {
          "$ref": "#/definitions/stop"
        }
      },
      "required": [
        "interface"
      ],
      "anyOf": [
        {
          "required": [
            "start"
          ]
        },
        {
          "required": [
            "stop"
          ]
        }
      ]
    }
  },
  "oneOf": [
    {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/definitions/switch_controllers_object"
      }
    },
    {
      "$ref": "#/definitions/switch_controllers_object"
    }
  ]
}